# B4Code static clickable demo starter

Open `index.html` in a browser. No build step or backend is required.

Purpose: give the developer a direct implementation reference for the clickable demo using the ROAD-002/003/004/005 scope. This is not production code.

Recommended next step: port this structure to the chosen frontend stack, preferably React/Next/Vite, and replace mock data with services only after demo flow is accepted.
